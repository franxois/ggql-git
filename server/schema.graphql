type Project {
  id: ID!
  name: String!
  currentBranch: String
  branches: [String]
}

type Query {
  projects: [Project!]!
  project( name: String! ): Project
}

# type Mutation {
#   createTodo(text: String!): Todo!
# }
